# For running the notebooks locally. (On Colab the first cell installs leaspy
# with --no-deps to reuse the preinstalled numpy/torch — see any notebook.)
# v2.1.0 is not on PyPI yet, so we install it from the git tag to match Colab.
leaspy @ git+https://github.com/aramis-lab/leaspy.git@v2.1.0
ipywidgets
pyyaml
matplotlib
jupyterlab
